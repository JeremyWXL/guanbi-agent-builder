# 基线 v4.3.0（2026-09-26 · tester v1.5.0）

- 版本：guanbi-agent-builder **v4.3.0（快速模式 lite 轨道）** · 单测 153 例全绿
- 环境：guancli 1.0.55 + default 环境（app.guandata.com，消费品行业演示域）

## L0 脚本冒烟（19 项）

19/19 通过。L0-2 含 `--mode lite` 流转；L0-19 数据集直学本次走看板路径未触发（按设计）。首轮双闸门 10 个 ❌ 真实拦截后全部修复，复检零 ❌。

## L1-lite 快速模式五步 E2E（本次主线）

**通过**。模拟用户决策全程显式记录（见 `_preview/_sandbox-v4.3.0/test-report.md`）：

- 模式选择：快速模式，状态机落盘 `mode: "lite"`
- L1：适检 3 ⚠️（2 张评分低带 🔴），**提醒义务执行后用户坚持继续**，风险登记
- L2：3 页 168 卡 → cards.json 119 张（带 _meta + filtered 分级，1 张合计不闭环禁用）；93/98 采样；双种子成稿
- L3：5 条冲突全部拍板（同比/环比=收款口径、达成=年累计/年目标、客户数=有毒禁用、大区费比=缓议）；metrics 59+9 rejected、dims 42 种子成稿；**双闸门零 ❌**
- L4：3 个典型问题入 examples.json；诊断链五条铁律保留
- L5：Q1-Q3 每场景 ≥1 题实测（归因走 attribute.py 闭环）；**交付包与完整版同构**（memory/ 四文件 + references 全套 + workbench.html）；包内 eval_examples 3/3
- 深化通道：preflight/next 提示深化 → 回完整版第 4 步逐条确认（订正年度目标字段、收编值别名 2 条）→ 双闸门复检零 ❌ → 标注改"已逐条确认" → **memory/ md5 未变、包结构零改动**

## L2 交付答题测评

本次未重复跑（lite 首测主线在搭建侧；交付包结构同构、L2 参照语料无变化，沿用 v3.7.0 基线结论）。

## 实机发现（亮点）

1. **隐藏筛选 + 动态参数双重陷阱实抓**：《近13个月指标渠道占比趋势》filtered=true + DYNAMIC_PARAMS，首轮 SQL 全表对比偏差 >50%；复写月份筛选 + SUM(收入) 后 **0% 误差闭环**——红线 10（SQL 必须复写隐藏筛选）的真实案例，已写入交付包
2. 双闸门在 lite"种子成稿"模式下不降低质量下限：首轮 10 ❌ 全部真实拦截
3. eval_examples 容差行为符合文档（亿级单位/聚合值格式），期望写法教训已记录

## 问题清单

| 级别 | 问题 | 状态 |
|---|---|---|
| P2 | run_sql 裸中文列名需反引号，sql-guide 可补明示 | open |
| P2 | check_formulas --seed-metrics 待裁决计数与 conflicts 口径不一致 | open |
| P2 | 33 个维度 values 为空（截断采样未覆盖） | tracking |

## 与上版基线差异（v3.7.0 → v4.3.0）

- 测试轨道新增 L1-lite（快速模式五步），完整版 L1 检查点表不变
- L0 17 → 19 项（L0-18 记忆系统、L0-19 数据集直学）
- 单测 24 → 153 例
- 无回归焦点；v3.7.0 三个 fixed 问题保持修复状态
