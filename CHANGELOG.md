# 变更日志

## v3.7.1（2026-09-25）沙箱全流程实测修复 + 独立测试 Agent

v3.7.0 经独立测试 Agent 沙箱全流程验证（L0 脚本 17 项 + L1 八步 E2E + L2 交付 agent 答题 94/100 等级 A，基线见 `tests/baselines/v3.7.0.md`），修复实测发现的 4 个问题：

- **修复 SKILL.md 第 2 步 parse_page 调用误导（实测踩中）**：原文"对每张看板执行"易被执行为逐张循环调用，cards-raw.json 互相覆盖静默丢看板；改为明示单次调用传入全部 pageId
- **SQL 直查探活前置（第 7 步）**：第 2 步探查结论会过期（实测演示域文件型数据集学习时可用、验收时已失效返回 0 行）；第 7 步 SQL 测试前必须先探活，失效则降级卡片间交叉验证，交付时 SQL 能力标注以第 7 步实测为准（第 8 步同步）
- **eval_examples.py 数值解析支持货币符号**：`￥1,234` / `¥71万` / `$3.5亿` 前缀不再导致解析失败（NUM_TOKEN 与 num_candidates 同步）
- **eval_examples.py 交付包路径回退**：examples.json 缺失于工作目录根时自动回退 `references/examples.json`，fetch.file 相对 examples.json 所在目录解析——交付包根目录 `python3 references/eval_examples.py .` 直接可用，无需软链
- **新增 `tests/agent-tester/` 独立测试 Agent**：L0 脚本冒烟矩阵（17 检查点）/ L1 八步向导 E2E 检查点与红线巡检 / L2 交付 agent 六类考题 + rubric.md 评分细则（7 条一票否决红线 + 四维度百分制），后续迭代用同一标准回归

## v3.7.0（2026-09-25）交互体验第四批：维度档案与取值消歧（P1.7）

解决的问题：用户对维度提问时（"华东怎么样""各大区对比"），助手缺少机器可读依据判断"说的是哪个维度、哪个成员值"——维度叫法相近（销售大区/财务大区/战区）、成员值跨维度重名（"华东"同时是两个大区的成员）、用户口头取值与档案值不完全一致（"华东区" vs "华东"），此前全靠 LLM 猜。

- **`dimensions.json` 机器可读维度档案**（模板 `references/templates/dimensions.json`，第 4 步确认产物，metrics.json 的姊妹档案）：每个维度固化 name（标准名）/ field（数据集字段）/ synonyms（用户叫法）/ values（成员枚举值候选池）/ valueAliases（相似值归一，"华东区"→"华东"）/ similarTo（易混维度）/ parent（层级，供归因下钻）
- **`check_dims.py` 校验闸门 + 种子生成**：
  - `--seed-dims`（第 2 步）：从卡片行维度/筛选器关联字段/指标 dims 收割维度清单，从 card-data 列画像合并枚举值，名称互为包含的维度自动标 similarTo 建议——第 4 步的起草底稿
  - 校验（第 4 步闸门，❌ 未清零禁止进入第 5 步）：结构 / 维度间 name·synonyms 撞车 / **与 metrics.json 指标用词撞车**（"门店"既是维度叫法又是指标名 = 问数路由歧义）/ 值别名悬空（valueAliases 目标值未收录 values）；⚠️ 警告：**值跨维度重叠清单**（交付后必须选项式消歧的取值）、同维度相似值（归一化后相同或互为包含，建议收编 valueAliases）、指标 dims 未收编、维度未收录成员值
- **交付助手「维度与取值消歧」协议**（agent-SKILL.md 对话体验规范）：定维度（synonyms 归一，多命中选项式消歧）→ 定取值（values 精确 → valueAliases 归一 → 包含/相似候选 ≤5 点选；查无此值给最接近候选，禁止静默空答/编造成员值）→ 值跨维度（上下文能定则定，定不了选项式问）；insightThinking.md 场景一意图解析同步
- **维度纠正双写回闭环**（agent-SKILL.md 维护）：用户纠正维度叫法/取值（"我们说的区域其实是战区"）→ 复述确认 → 写回 dimensions.json → check_dims.py 校验 → "已记住"，与口径纠错同规格
- **接线**：SKILL.md 第 2 步新增 --seed-dims 种子、第 4 步新增维度档案确认（叫法/易混分工/值别名三件事拍板）、第 8 步交付包纳入 dimensions.json + check_dims.py；`test_check_dims.py` 10 例单测入 CI
- 留作后续：workbench 维度档案表格视图（对齐 v3.5.0 指标档案视图）

## v3.6.0（2026-09-25）交互体验第三批：看板筛选直达链接（P1.6，已实机验证）

- **`make_link.py` 筛选直达链接生成器**：把筛选条件拼进 BI 看板 URL（观远 BI 页面原生支持 `?筛选器ID=值` 查询参数；多值重复传参、`--anchor` 定位卡片、`--list` 列出可用筛选器），用户点开就是筛好条件的视图——归因定位到"华东拖后腿"时，回答末尾直接附"点这里看华东的看板"。字段没有页面筛选器时明确报错并列出可用字段，禁止静默忽略或手拼 URL
- **实机验证结论**（app.guandata.com，页面「1｜集团总览」`?月份筛选器cdId=2020-04`：销售总额 636M→550M，取数请求 payload 确认 `sourceCdId` 带筛选）：
  - **筛选器"线上 ID" = 卡片 cdId**，`guancli page get --raw` 直接可得，无需进编辑界面"复制筛选器ID"（与编辑 UI 复制结果逐一核对一致；编辑模式 draft API 的 `originCdId` 交叉验证）
  - **同名筛选器必须选 `meta.filterLayout` 里的那个**（筛选栏成员）——页面可能存在不在筛选栏的同名筛选器，之前三次实机失败的根因就是拿错了 ID
  - **只暴露有联动的筛选器**：`settings.asFilter.targetCdIds` 为空的筛选器只改 UI 不改数据，不作为直达链接候选
  - **FIRST_PICK 降级**：筛选栏含 `defaultValue.valueType == "FIRST_PICK"`（首选项）筛选器的页面，首屏取数在 first-pick 异步解析前发出（filters 为空）且不再重查，URL 参数只填 UI 不进数据层，"查询"按钮保持 disabled——此类页面生成链接时明确告警
- **parse_page.py 筛选交互学习**：cards-raw.json 的筛选器卡片新增 `inFilterBar`（是否在筛选栏）、`linkedCardCount`（联动卡片数）、`defaultValueType`（FIRST_PICK/FIXED_VALUE/ALL）、`multiSelect`、`selectorType` 字段——交付后 make_link.py 选候选、判降级全靠它们，也为交付助手理解页面筛选交互提供原料
- **接线**：agent-SKILL.md 对话体验规范新增"看板直达链接"（筛选值必须来自取数结果/枚举值，禁止编造）；SKILL.md 第 2 步要求 cards.json 保留筛选器 cdId 与关联字段名（链接原料），第 8 步交付包纳入 make_link.py
- 版本号说明：v3.4.0 曾预留给本特性（现场在 git stash），因 v3.5.0 先行发布，本特性以 v3.6.0 落地

## v3.5.0（2026-09-25）工作台指标档案表格视图（P3）

- **metrics.json 表单化**：业务口径页顶部新增"指标档案"区块——结构化指标（标准名/算法/别名/维度/单位/换维度安全性/来源）从裸 JSON 变成台账式卡片，换维度安全性翻译成人话（"换维度须明细现算·去重"），已排除候选（rejected）折叠展示
- **表单化编辑**：编辑模式下每行可直接改名称/算法/别名/维度/单位/换维度/来源/备注（下拉+输入框，不再是手改 JSON），支持新增/删除指标；保存即写回 metrics.json 并刷新 updatedAt，自动备份不变
- 版本号说明：v3.4.0 预留给 P1.6 筛选直达链接（待实机验证，现场在 git stash），本版本号跳过

## v3.3.1（2026-09-25）交互体验第二批：交付助手对话体验规范

agent-SKILL.md 模板新增「对话体验规范」一节，insightThinking.md 场景一同步：

- **首次对话自我介绍**：首次触发或用户问"你能干什么"时，主动说明覆盖看板、四类能力与示例问题（示例优先从 examples.json 已验收条目挑，示范即验证过的）
- **数据出身行**：凡回答带数字，末尾固定附"来源：《看板》卡片 · 取数于 XX:XX"；学习时采样与实时取数明确区分
- **模糊问题选项式消歧**：一次只问一个最关键歧义点 + 候选选项点选，不做开放式反问；多歧义时先问影响最大的，其余用默认值并声明
- **超范围问题给出路**：说明覆盖范围后指出"哪张看板能补上、要不要加进来"，把死胡同变成增量学习入口；禁止只答"我不知道"

## v3.3.0（2026-09-25）交互体验主线第一批：纠错闭环 + 异议驱动确认 + 进度播报

- **口径纠错对话闭环**（交付助手，模板 agent-SKILL.md）：用户在对话中纠正口径（"不对，应该除以 GMV"）→ 复述确认 → 同时写回 metrics.json 与 businessKnowledge.md → check_metrics.py 校验 → "已记住，以后都这么算"。纠错从维护任务变一句话，口径档案越用越准
- **口径确认改异议驱动**（搭建第 4 步）：共识无冲突的条目按主题分组默认采纳（"有错请指出"），只有冲突项与疑似问题置顶逐条拍板——确认量从几十条降到几条，"口径必须确认"红线不破
- **进度播报**（搭建全程）：每步开头一行"第 N 步/共 8 步 · 这一步做什么 · 大约还要多久"，断点续建同样播报；第 3 步跳过时按剩余步数折算
- **P0 文档同步**：agent-SKILL.md 模板补齐 v3.1/v3.2 能力——体检段更新为结构指纹版（具体增删报告/复核阈值/脚本升级提示），口径来源加入 metrics.json（synonyms 别名归一 + 不一致时以 metrics.json 为准），批量调口径必经 check_metrics.py

## v3.2.1（2026-09-25）workbench.py 工程化 + CI

- **最小单测落地**（`references/scripts/test_workbench.py`，10 例）：结构指纹稳定性与跨脚本一致性、版本元组比较、卡片增删改名 diff、check_staleness 全路径（具体增删报告/配置未变/旧版档案降级/看板失联）、collect() 数据收集、保存回调白名单与自动备份——全部打桩不触网
- **服务层抽离**：工作台保存回调从 serve_single 闭包抽出为 `make_save_file(workdir)`，可独立测试
- **CI 上线**（GitHub Actions）：push/PR 自动跑 py_compile 全脚本 + 单测 + frontmatter 必填字段 + 四处版本号一致性（SKILL.md / kimi.plugin.json / parse_page.py / workbench.py——版本漂移从此 push 即拦）

## v3.2.0（2026-09-25）businessKnowledge 结构化：机器可读口径档案

- **`metrics.json` 机器可读口径档案**：businessKnowledge.md（人读台账）之外新增结构化孪生——每条确认的口径固化为 name/formula（或 baseField+aggrType）/dims/synonyms/safety/source/示例问题，字段设计向 OSI（开放语义交换标准）对齐，口径档案从此可被脚本校验、可跨工具存活；确认过但不收编的候选写入 rejected（含原因），防止重新学习时重复提问
- **校验闸门 `check_metrics.py`**（第 4 步新增必跑项）：结构校验（name 唯一/formula 或 baseField 必填/pending 标记未清）+ 同义词撞车检测（撞车 = 问数路由歧义，❌）+ 冲突裁决闭环（formulas.json 的冲突必须在 metrics 或 rejected 有着落，❌）+ 公式一致性/候选未收编/维度未见（⚠️）；❌ 未清零禁止进入第 5 步
- **种子生成 `check_formulas.py --seed-metrics`**（第 2 步）：共识指标预填公式/safety/维度直接成稿，冲突指标标 pending 待裁决——第 4 步从"逐条起草"变"逐条过稿"
- **工作台**：概览统计行与多 agent 总览卡片新增"N 项指标"计数

## v3.1.0（2026-09-25）资产体检升级 + builderVersion 升级通道

- **结构指纹双信号体检**：`_meta.pages` 新增每看板卡片结构指纹（cardHash）与卡片清单（cdId+名称），体检从"mtime 变了"升级为具体报告——"《经营驾驶舱》新增 2 张卡片（退款分析、费用趋势）、删除 1 张（门店排行）、改名 1 张（毛利率→综合毛利率）"；卡片清单未变时报"配置/布局调整"，避免误报吓到用户；旧版档案无指纹时降级为 mtime 对比并注明无法具体对比
- **复核阈值可配置**：距上次学习超过 N 天（默认 30，`--fresh-days` 可调）即使看板未变也提醒复核口径——口径漂移不一定伴随看板改版
- **builderVersion 升级通道**：parse_page 学习时把 builder 版本号写入 `_meta.builderVersion`；体检对比交付包版本与当前脚本版本，落后时提示"运行脚本可升级 vX → vY"；多 agent 总览页卡片直接显示"脚本可升级"徽章，不用逐个点开体检
- **体检性能**：多看板当前快照并发获取（ThreadPoolExecutor，与 parse_page/check_pages 同风格）

## v3.0.0（2026-09-25）质量护城河：示例库 + 回归评测 + 归因引擎 + 断点状态机

- **examples.json few-shot 示例库**：第 5 步确认的典型问题 + 第 7 步验收通过的答案固化为结构化示例（问题 → 路由看板/卡片 → 取数方式 → 期望数值与结论要点 + 人工确认状态位），交付时随 agent 包带走，作为回答参照语料与回归基准（对齐 Vanna 语料三分法，补齐"示例对"一环）
- **回归评测 `eval_examples.py`**：看板改版重新学习后一键重跑示例库——数据层自动核验（期望值容差匹配，容忍千分位/百分号/中文单位"万/亿"），结论要点逐条打印为人工核对清单，按场景统计通过率；run_sql 不可用时记 skip 而非 fail；采样文件缺失提示"看板可能已改版，需要重新学习"
- **归因计算引擎 `attribute.py`**：加法拆解（维度成员对总变化的贡献额/贡献率排序，Σ 闭环验证）与乘法拆解（"店数×单店"类，2~4 因子 Shapley 连环替代——与因子顺序无关且精确闭环）脚本化；LLM 只解释计算结果，禁止口算贡献占比；数值解析容忍千分位/百分号/中文单位
- **断点状态机 `wizard_state.py` + preflight 断点检测**：wizard-state.json 显式记录八步进度（pending/in_progress/confirming/confirmed/skipped；第 4、7 质量命门步禁止 skipped），写操作原子化（临时文件 + rename）；preflight 传入工作目录时自动检测未完成搭建并打印续建摘要（"上次进行到第 N 步"），替换"从落盘产物猜进度"

## v2.7.0（2026-09-25）看板适检 + 指标口径字典

- **看板适检 `check_pages.py`（第 1 步新增环节）**：勾选后、学习前对看板做快速适配检查（每张看板一次接口调用、并发数秒完成），输出 ⛔ 不可用（取不到/大屏/导航页/0 数据卡/数据集全不可读）/ ⚠️ 有风险（重名卡多、数据停更、卡名锁死固定期间、卡太少、疑似演示备份页）/ ✅ 适合三档结论，不合适的看板在投入学习前拦下来
- **指标公式收割（parse_page.py 扩展）**：每张数据卡新增行维度（dims）与度量明细（measures：聚合方式/计算公式/同比占比高级计算参数/fdId）；自动并发收割各数据集的计算字段公式（`ds get --raw --brief` 轻量路径，`_meta.dsFormulas`，`--skip-ds-formulas` 可关闭）；看板解析改为并发执行
- **口径字典 `check_formulas.py`（第 2 步新增产物 formulas.json）**：汇总卡片与数据集公式，按公式结构标注换维度聚合安全性（自由重聚合/需明细现算/行级逻辑/时间宏）；同名指标公式不一致自动生成冲突清单（实测一张看板抓出 5 处真冲突，如"退款率"分母收入 vs GMV），带入第 4 步请用户裁决；自动生成的口径候选条目（含卡片隐藏筛选上下文）作为第 4 步确认素材
- **SQL 直查三条新红线（全部实测验证）**：禁止 AVG(明细比率)（实测 105.01% vs 正确 103.60%，错得不多最难察觉）；禁止按名引用数据集计算字段（SQL 引擎只认物理列，报 UNRESOLVED_COLUMN，必须内联公式）；SQL 必须显式复写卡片隐藏筛选（漏掉 `时间维度='月累计'` 实测差 259 倍）
- **修复 run_sql.py 误拦截**：`ORDER BY ... DESC` 被 DESCRIBE 防护规则误伤，已放行（DESCRIBE 仍拦截）

## v2.6.0（2026-09-24）工作台视觉重设计

- **校验工作台 UI 全面重设计**：从"卡片堆叠的后台模板"改为「档案 × 控制台」设计语言——暖纸面底色 + 细线分节替代层层卡片；等宽字体微型标签（AGENT DOSSIER / SOURCE / DIAGNOSTIC）+ 大标题的编辑式层级；LED 状态灯贯穿模式徽章、体检结果与状态图例；导航改为 01–05 编号目录式（hover 强调线展开）；业务头像改为名称首字符纯色 monogram（去掉 emoji 与渐变）
- **业务口径台账化**：规则按 01–N 台账编号排版，自动识别并展示【确认】/【看板】/【SQL】来源标签与 ⚠️ 注意标记，归因公式条目左侧强调条高亮；兼容 `**N.【标签】…**` 整行加粗的旧版编号格式（此前会整段粘连成一条规则）
- **旧版数据资产结构兼容**：cards.json 的 `{pages: {...}}` 字典、`{pages: {...}, cards: {"看板__卡片名": {...}}}` 平铺字典与 `[{name, pageId, cards: [...]}]` 列表三种历史结构降级展示数据源与卡片（此前概览页会把 meta/pages 当成看板显示；平铺结构的 90 张卡片会被整体丢弃显示 0 张）；多 agent 总览页同步补齐旧版结构的看板/卡片统计
- **markdown-lite 渲染增强**：支持管道表格（等宽表头 + 细线行）与 `##` 子标题
- **新增 `?light` / `?dark` URL 参数**：强制浅色/深色主题，覆盖系统偏好

## v2.5.0（2026-09-24）健壮性版本

- **看板解析改走结构化 JSON**：`parse_page.py` 主路径改用 `guancli page get --raw`，卡片名称/ID/类型天然配对，不再依赖脆弱的文本正则；`--raw` 不可用时自动回退文本解析（保留原有的块内配对纪律）。新增哨兵：原始数据有卡片却解析不出时直接报错退出，禁止带着空资产继续。回退路径同步修复两处静默丢卡的 bug：位置标记只认 `[卡片池]`（实际还有 `[未布局/未知位置]`、`[筛选器区域]`，v2.4 实测 51 张卡被静默丢掉 28 张）、空命名卡片被正则跳过
- **解析产物更丰富**：每卡新增所属数据集 ID（dsId，SQL 直查探查不再靠猜）、详细图表类型、筛选器字段名与取值（fdId 自动翻译成可读名称）、单位线索（prefixUnit/divideDataBy，如"万"）、卡片池标记改由 `backlogLayout` 精确判定
- **修复隐性 bug**：`sample_cards.py` 遇到 cards-raw.json 的 `_meta` 键会直接 KeyError 崩溃（v2.4 引入 `_meta` 后未同步），已修复为自动跳过
- **采样截断 + 列画像**：每张卡默认最多落盘 200 行（防大卡片撑爆上下文，原始行数记入索引）；`_sample_index.json` 新增每列画像——数值列 min/max、低基数文本列枚举值（防筛选取值幻觉）、空列识别；截断卡片的合计闭环校验自动跳过
- **只读 SQL 执行器 `run_sql.py`**：`guancli ds execute-sql` 的安全包装，脚本层强制单条 SELECT/WITH 查询，拦截多语句与写操作关键字——prompt 红线之外加一道硬保险；搭建与交付后的 agent 统一走此入口
- **前置检查脚本 `preflight.py`**（第 0 步）：一键检查 guancli 安装/版本、BI 认证、看板树与看板详情的 JSON 结构探针，提前暴露版本兼容问题
- **文档修正**：步骤编号统一为第 0–8 步的"八步向导"（修复测试验收回退指引中"口径回第 3 步、框架回第 5 步"的错误引用）；frontmatter description 瘦身，版本历史移至本文件

## v2.4.0（2026-09-24）

- 工作台概览页（agent 身份卡 + 看板一键跳转 BI）
- 资产体检：对比看板学习时点与当前更新时间，过期提醒重新学习
- 多 agent 管理总览页（`--agents`，业务头像 + 独立卡片 + 逐个体检）

## v2.3.x（2026-09-23）

- v2.3.0：校验工作台——资产目录/业务口径/分析思路变成浏览器里可查看、可直接编辑的本地页面（保存自动备份 `.bak-<时间戳>`）；交付包含只读工作台 workbench.html
- v2.3.1/v2.3.2：工作台 UI 重设计（WorkBuddy 原生风格）与视觉交互优化

## v2.2.0（2026-09）

- 勾选式看板选择：自动列出用户有权限的仪表板供点击勾选/编号勾选
- 全部确认点提供"✅ 就这样 / ✏️ 改某条 / ⏭️ 跳过"结构化出口

## v2.1.0（2026-09）

- 项目转为开源（MIT）：封存 PaySkill 收费版至 `archive/`，基于免费版 v2.0.0 延续迭代

## v2.0.0（2026-09）

- 行业认知预研 + 历史报告提炼 + 历史对话沉淀（第 3 步，可选），解决"裸看板搭建、缺业务输入"的核心短板
- 诊断链方法论注入 insightThinking（对齐观远原生 agent 的动态下钻纪律）

---

更早历史：v1.0.0–v3.0.1 为 PaySkill 收费版，已封存于 [archive/](archive/README.md)。
